# Where Is Neo? & ForgematicaPrinter 联合操作手册

> NeoForge 1.21.1 纯客户端 — 物品定位 + 容器追踪 + 投影材料准备 + 自动建造

---

## 模组依赖关系

```
Minecraft 1.21.1 + NeoForge
├── Where Is Neo? (WhereIsIt NeoForge 移植版)
│   └── [必需] YACL — 配置界面框架
│
├── ForgematicaPrinter (投影打印机)
│   ├── [必需] Forgematica — Litematica NeoForge 移植版
│   │   └── [必需] MaFgLib — MaLiLib NeoForge 移植版
│   │       └── [可选] BadPackets — 部分版本需要
│   └── [运行时] Forgified Fabric API — 部分网络模块
│
└── [可选] Xaero's Minimap / WorldMap — 小地图（快捷键不冲突）
```

### 下载链接

| 模组 | 平台 | 说明 |
|------|------|------|
| **Where Is Neo?** | 本地构建 | 物品定位与容器追踪 |
| **ForgematicaPrinter** | 本地构建 | 投影自动建造 |
| [YACL](https://modrinth.com/mod/yacl) | Modrinth | Where Is Neo? 前置 |
| [Forgematica](https://modrinth.com/mod/forgematica) | Modrinth | Litematica NeoForge 移植版 |
| [MaFgLib](https://modrinth.com/mod/mafglib) | Modrinth | MaLiLib NeoForge 移植版，Forgematica 前置 |
| [BadPackets](https://modrinth.com/mod/badpackets) | Modrinth | MaFgLib 部分版本需要 |

### 安装步骤

1. 确保已安装 Minecraft 1.21.1 + NeoForge
2. 将所有 jar 文件放入 `.minecraft/mods` 文件夹
3. **必须安装：** MaFgLib → Forgematica → ForgematicaPrinter（按顺序）
4. **必须安装：** YACL → Where Is Neo?
5. 启动游戏

> **注意：** MaFgLib 是 Forgematica 的硬性前置，不安装 Forgematica 会崩溃。Where Is Neo? 不依赖 Forgematica，但安装后可启用投影材料准备功能。

---

## 快捷键总览

| 功能 | 按键 | 所属模组 |
|------|------|---------|
| 搜索物品 | `Y` | Where Is Neo? |
| 打开物品浏览器 | `G` | Where Is Neo? |
| 投影主菜单 | `N` | Forgematica |
| 原理图检查器 | `N+V` | Forgematica |
| 材料清单 | `N+L` | Forgematica |
| 开启/关闭打印 | `N+C` | ForgematicaPrinter |
| 切换毁天灭地模式 | `N+M` | ForgematicaPrinter |

> **提示：** Forgematica 默认快捷键可能与小地图冲突，建议在配置中改为 `N` 前缀。

---

## 完整工作流：从投影到建造

### 第一步：加载投影

1. 使用 Forgematica 加载一个 `.litematica` 投影文件
2. 调整投影位置和方向
3. 确认投影已放置到目标位置

### 第二步：查看材料需求

1. 按 `N+L` 打开 Forgematica 的材料清单
   - **这一步必须执行！** Where Is Neo? 需要从这里读取材料数据
2. 浏览材料列表，了解需要哪些物品
3. 关闭材料清单界面

### 第三步：定位材料容器

1. 按 `G` 打开 Where Is Neo? 物品浏览器
2. 点击右上角 **[投影]** 按钮，打开投影材料准备界面
3. 界面显示每个材料的准备状态：`存量(百分比%)/需求量`

```
┌──────────────────────────────────────────────────┐
│    Where Is Neo? - 投影材料准备 - 我的小屋        │
│ < 返回                                           │
│ 物品 ↕           │ 准备状态 ↕       │ 追踪 │ 完成 │
│ ──────────────────────────────────────────────── │
│ 🔴 铜锭          │ 0(0%)/32        │ ...  │ ✗   │  ← 最缺的排最前
│ 🟡 石砖          │ 64(80%)/80      │ ...  │ ✗   │
│ 🟢 橡木木板      │ 128(160%)/80    │ ...  │ ✗   │  ← 超量显示绿色
└──────────────────────────────────────────────────┘
```

4. 点击不足材料的 **追踪** 按钮，Where Is Neo? 会在附近容器中高亮该物品
5. 右上角 HUD 显示追踪进度：`[图标] 物品名 已取出(容器数)/总量`
6. 前往高亮的容器取出材料

### 第四步：标记完成

- 材料备齐后，点击 **完成** 按钮（`✗` → `✓`），该行变灰并加删除线
- 行不会被隐藏，方便回看
- 再次点击可取消完成标记

### 第五步：自动建造

1. 确认所有材料已备齐
2. 按 `N+C` 开启 ForgematicaPrinter 打印模式
3. 打印机自动在范围内放置方块
4. 打印完成后按 `N+C` 关闭

---

## Where Is Neo? 详细操作

### 搜索物品（Y 键）

| 场景 | 操作 | 说明 |
|------|------|------|
| 容器界面中 | 悬停物品 + `Y` | 搜索附近容器中的该物品 |
| 容器界面中 | 悬停物品 + `Shift+Y` | 精确搜索（匹配附魔/NBT） |
| 无界面时 | `Y` | 搜索手持物品（需配置开启） |
| 配方书中 | 悬停材料 + `Y` | 搜索该材料/标签 |

### 物品浏览器（G 键）

```
┌─────────────────────────────────────────────────┐
│        Where Is Neo? - Neoforge 1.21.1          │
│ [搜索框]                        [投影] [清除]    │
│ 物品 ↕          │ 来源 ↕        │ 分布 ↕  │追踪│
│ ─────────────────────────────────────────────── │
│ 🧱 石砖         │ Minecraft     │ 64(2箱) │ .. │
│ 🔶 铜锭         │ Minecraft     │ 32(1箱) │ ✓  │
│ ...                                              │
└─────────────────────────────────────────────────┘
```

| 操作 | 说明 |
|------|------|
| 搜索框 | 输入物品名过滤列表 |
| 点击列头 | 切换排序（物品名按拼音，来源/分布按数量） |
| 点击物品行 | 切换追踪该物品 |
| **[投影]** 按钮 | 打开投影材料准备界面（需安装 Forgematica） |
| **[清除]** 按钮 | 清空所有容器记录（点击两次确认） |

### 多选追踪系统

- 最多同时追踪 **5** 个物品
- 每个物品分配独立的随机颜色闪烁高亮
- 右上角 HUD 实时显示：`[图标] 物品名 已取出(容器数)/原始总量`
- 原始总量 = 当前容器剩余 + 已取出量，取出后分母同步变化
- 开始追踪时自动清零已收集数据
- 结束追踪时自动清理已收集数据

### 容器记录原理

Where Is Neo? 是纯客户端模组，通过记录你打开过的容器内容来工作：

1. **右键容器** → 记录位置和方块状态
2. **打开容器界面** → 关联到右键的位置
3. **关闭容器界面** → 快照当前内容并保存
4. **搜索/浏览时** → 从保存的记录中查询

> **重要：** 必须打开过容器才能搜索其中的物品。关闭容器时才保存快照。

---

## ForgematicaPrinter 详细操作

### 打印模式（建造投影）

1. 加载一个投影（Forgematica 加载 .litematica 文件）
2. 用 Forgematica 选区工具框选要打印的区域
3. 按 `N+C` 开启打印模式，或按住 `N` 持续打印
4. 打印机自动在范围内放置方块

### 毁天灭地模式（清除区域）

1. 用 Forgematica 选区工具框选要清除的区域
2. 按 `N+M` 切换到毁天灭地模式
3. 按 `N+C` 开启打印模式
4. 打印机自动破坏范围内所有方块
5. 停止破坏 30 秒后，聊天栏显示破坏统计

### 关键配置

| 配置项 | 默认值（推荐值） | 说明 |
|--------|--------|------|
| 打印间隔 | 12(1) | 每次动作间隔 tick 数，0=最快 |
| 打印范围 | 5(5) | 放置/破坏的最大距离 |
| 每tick放置数 | 1 | 每tick最大放置/破坏数 |
| 最大放置尝试次数 | 5(3) | 超过此次数后跳过该方块 |
| 交互方块 | 开启 | 自动右键交互（拉杆、门等） |
| 破坏错误状态方块 | 开启 | 自动破坏朝向错误的方块 |

---

## Where Is Neo? 配置说明

通过 `Esc → 选项 → 模组列表 → Where Is It → 配置` 访问。

### 通用设置

| 配置项 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| 搜索范围 | 8 | 4~24 | Y 键搜索的半径（格） |
| 追踪范围 | 128 | 16~1024 | 追踪模式扫描容器的半径（格） |
| 追踪刷新间隔 | 5 | 1~40 | 追踪重新扫描间隔（tick） |
| 最大追踪物品数 | 5 | 1~10 | 同时追踪的物品上限 |
| 显示追踪 HUD | 开启 | — | 右上角追踪信息显示 |
| 追踪 HUD 缩放 | 1.0 | 0.5~2.0 | HUD 显示大小 |
| 高亮淡出时间 | 15秒 | 5~30秒 | 搜索结果高亮持续时间 |

### 客户端设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 手持物品搜索 | 关闭 | 无界面时按 Y 搜索手持物品 |
| 搜索音效 | 关闭 | 触发搜索时播放声音 |
| 找到后关闭界面 | 开启 | 搜索到结果后自动关闭容器界面 |
| 槽位高亮 | 开启 | 在容器界面中高亮匹配的槽位 |
| 显示容器名称 | 开启 | 在搜索结果旁显示容器名 |

---

## 常见问题

### Where Is Neo? 相关

**搜索没有结果**
- 确认你打开过包含该物品的容器
- 检查搜索范围是否足够大（默认 8 格）

**投影材料界面列表为空**
- 确认已安装 Forgematica
- **必须先按 `N+L` 打开一次 Forgematica 材料列表**
- 确认已选中一个投影放置

**追踪高亮不显示**
- 确认追踪已激活（HUD 中显示追踪状态）
- 检查追踪范围是否覆盖目标容器
- 确认配置中"槽位高亮"已开启

**追踪 HUD 数据异常**
- 追踪开始/结束时自动清理数据
- 如仍有残余，点击物品浏览器的 `清除` 按钮重置

### ForgematicaPrinter 相关

**打印机卡住不动**
- 减小最大放置尝试次数（默认 5，改为 2-3）
- 增大卡顿检测阈值或关闭卡顿检测
- 减小错误状态最大破坏次数（默认 3，改为 1-2）

**打印速度太慢**
- 减小打印间隔（默认 12，改为 0-5）
- 增大每tick放置数（默认 1，创造模式可设 3-5）

**被服务器踢出**
- 减小打印速度和每tick放置数
- 部分服务器禁止自动建造，请确认服务器规则

### Forgematica 相关

**投影不显示/渲染异常**
- Forgematica 的渲染在 1.21.3+ 与光影不兼容
- 使用投影时请关闭光影
- 推荐使用 Sodium + Iris 替代 OptiFine

---

## 数据存储位置

| 数据 | 路径 | 说明 |
|------|------|------|
| Where Is Neo? 配置 | `config/whereisit.json5` | YACL 格式配置 |
| 容器记录 | `config/whereisit/records/<世界标识>.dat` | 每个世界独立保存 |
| Forgematica 配置 | `config/forgematica/` | 投影相关配置 |
| MaFgLib 配置 | `config/mafglib/` | 按键绑定等基础配置 |
