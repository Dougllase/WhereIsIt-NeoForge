# Where Is Neo? 操作手册

> NeoForge 1.21.1 纯客户端版 — 物品定位 & 容器追踪 & 投影材料准备

## 快速开始

### 1. 首次安装

将模组 jar 文件放入 `.minecraft/mods` 文件夹，启动游戏即可。

**前置依赖：**
- [YACL](https://modrinth.com/mod/yacl) — 配置界面框架

**可选搭配：**
- [Forgematica (Litematica NeoForge 移植版)](https://modrinth.com/mod/forgematica) — 启用投影材料准备功能
- [ForgematicaPrinter](./TUTORIAL.md) — 配合打印机实现自动建造

### 2. 快捷键

| 功能 | 默认按键 | 说明 |
|------|---------|------|
| 搜索物品 | `Y` | 在容器界面悬停物品时按 Y 搜索 |
| 打开物品浏览器 | `G` | 打开物品浏览器界面，查看所有已记录的容器内容 |

> **提示：** 快捷键可在 `Esc → 选项 → 按键绑定 → Where Is It` 中修改。

### 3. 打开配置菜单

- 游戏内按 `Esc` → 选项 → 模组列表 → 找到 **Where Is It** → 配置
- 或通过模组菜单（如 Mod Menu 兼容的模组列表）进入

---

## 基本操作

### 搜索物品（Y 键）

1. 打开任意容器界面（箱子、背包等）
2. 将鼠标悬停在要搜索的物品上
3. 按 `Y` 键，附近容器中包含该物品的位置会以高亮框显示
4. 按住 `Shift` 再按 `Y` 可精确搜索（匹配 NBT/附魔等）

**手持搜索：** 在没有打开界面的情况下按 `Y`，将搜索手持物品（需在配置中开启）。

### 物品浏览器（G 键）

按 `G` 打开物品浏览器，查看所有已打开过的容器中的物品汇总。

**界面布局：**

```
┌─────────────────────────────────────────────────┐
│        Where Is Neo? - Neoforge 1.21.1          │
│ [搜索框]                        [投影] [清除]    │
│ 物品 ↕          │ 来源 ↕        │ 分布 ↕  │追踪│
│ ─────────────────────────────────────────────── │
│ 🧱 石砖         │ Minecraft     │ 64(2箱) │ .. │
│ 🔶 铜锭         │ Minecraft     │ 32(1箱) │ ✓  │
│ ...                                              │
└─────────────────────────────────────────────────┘
```

**操作说明：**

| 操作 | 说明 |
|------|------|
| 搜索框 | 输入物品名过滤列表 |
| 点击列头 | 切换排序（物品名按拼音，来源/分布按数量） |
| 点击物品行 | 切换追踪该物品 |
| 点击 `追踪` 按钮 | 切换追踪（`✓` = 正在追踪） |
| 点击 `投影` 按钮 | 打开投影材料准备界面（需安装 Litematica） |
| 点击 `清除` 按钮 | 清空所有容器记录（需点击两次确认） |

### 多选追踪系统

最多可同时追踪 **5** 个物品，每个物品分配独立的随机颜色闪烁高亮。

**追踪方式：**
- 在物品浏览器中点击物品行或追踪按钮
- 在投影材料界面中点击追踪按钮

**追踪 HUD：** 追踪激活时，右上角（计分板上方）显示追踪信息：

```
[🧱] 石砖 32(2)/96
[🔶] 铜锭 64(1)/64
```

格式：`[图标] 物品名 已取出数(容器数)/原始总量`

> **注意：** 原始总量 = 当前容器剩余 + 已取出量，取出物品后分母会同步变化。

**追踪高亮：** 被追踪物品在容器中以对应颜色的脉冲闪烁显示，而非普通搜索的渐变彩虹效果。

---

## 投影材料准备界面

配合 Forgematica (Litematica) 使用，检查投影所需材料的准备情况。

### 打开方式

1. 按 `G` 打开物品浏览器
2. 点击右上角 `投影` 按钮

### 使用前提

1. 已安装 Forgematica (Litematica)
2. 已加载并选中一个投影放置
3. **必须先在 Litematica 中打开一次材料列表**（按 `N+L`），否则列表为空

### 界面布局

```
┌──────────────────────────────────────────────────┐
│    Where Is Neo? - 投影材料准备 - 我的小屋        │
│ < 返回                                           │
│ 物品 ↕           │ 准备状态 ↕       │ 追踪 │ 完成 │
│ ──────────────────────────────────────────────── │
│ 🧱 石砖          │ 64(80%)/80      │ ...  │ ✗   │
│ 🪵 橡木木板      │ 128(160%)/80    │ ...  │ ✗   │
│ 🔶 铜锭          │ 0(0%)/32        │ ...  │ ✗   │
│ ~~玻璃~~         │ 64(100%)/64     │ ...  │ ✓   │
└──────────────────────────────────────────────────┘
```

**准备状态列格式：** `存量(百分比%)/需求量`

| 颜色 | 含义 |
|------|------|
| 🔴 红色 | 准备不足 50% |
| 🟡 黄色 | 准备 50%~99% |
| 🟢 绿色 | 准备 100%+ |

**操作说明：**

| 操作 | 说明 |
|------|------|
| 点击 `追踪` 按钮 | 追踪该物品，在容器中高亮显示 |
| 点击 `完成` 按钮 (`✗`/`✓`) | 标记该物品为已收集完成，行变灰并加删除线 |
| 点击物品名 | 切换追踪 |
| 点击列头 `物品` | 按中文拼音排序 |
| 点击列头 `准备状态` | 按百分比排序（默认升序，最缺的排最前） |
| 点击 `< 返回` | 返回物品浏览器 |

> **存量数据来源：** 来自 WhereIsIt 记录的周围容器内容，需要先打开过附近的箱子才能统计。Litematica 自己的 "available" 是背包数量，这里用的是容器中的数量。

---

## 容器记录系统

### 工作原理

WhereIsIt 是纯客户端模组，通过记录你打开过的容器内容来工作：

1. **右键容器** → 记录容器位置和方块状态
2. **打开容器界面** → 关联到右键的位置
3. **关闭容器界面** → 快照当前内容并保存
4. **搜索/浏览时** → 从保存的记录中查询

### 数据持久化

- 容器记录保存在 `config/whereisit/records/<世界标识>.dat`
- 单人游戏：以存档名标识
- 多人游戏：以服务器 IP 标识
- 每 30 秒自动保存，退出世界时也会保存

### 注意事项

- **必须打开过容器**才能搜索其中的物品
- 关闭容器时才保存内容快照，如果内容变化后未重新打开，记录可能过时
- 双重箱子会自动合并为同一条记录
- 附魔台、砂轮、织布机等不保留物品的界面不会被记录

---

## 配合 ForgematicaPrinter 使用

WhereIsNeo 与 ForgematicaPrinter 配合使用可以实现完整的投影建造工作流：

### 推荐工作流

```
1. 加载投影 (Litematica)
     ↓
2. 打开材料列表 (N+L) 刷新材料数据
     ↓
3. 打开 WhereIsNeo 投影材料界面 (G → 投影)
     ↓
4. 查看哪些材料不足，点击追踪按钮定位容器
     ↓
5. 从容器中取出材料
     ↓
6. 标记已备齐的材料为"完成"
     ↓
7. 材料备齐后，使用 ForgematicaPrinter 自动建造
```

### ForgematicaPrinter 快速参考

| 操作 | 按键 | 说明 |
|------|------|------|
| 投影主菜单 | `N` | Litematica 主菜单 |
| 原理图检查器 | `N+V` | 检查投影结构 |
| 材料清单 | `N+L` | **必须先打开一次**才能在 WhereIsNeo 中显示 |
| 开启打印 | `N+C` | 开始自动建造 |
| 毁天灭地模式 | `N+M` | 清除区域 |

> 详细的打印机配置请参考 [ForgematicaPrinter 操作手册](./TUTORIAL.md)。

---

## 关键配置说明

通过 `Esc → 选项 → 模组列表 → Where Is It → 配置` 访问。

### 通用设置

| 配置项 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| 搜索范围 | 8 | 4~24 | Y 键搜索的半径（格） |
| 追踪范围 | 128 | 16~1024 | 追踪模式扫描容器的半径（格） |
| 追踪刷新间隔 | 5 | 1~40 | 追踪重新扫描间隔（tick），越小越实时但越耗性能 |
| 最大追踪物品数 | 5 | 1~10 | 同时追踪的物品上限 |
| 显示追踪 HUD | 开启 | — | 右上角追踪信息显示 |
| 追踪 HUD 缩放 | 1.0 | 0.5~2.0 | HUD 显示大小 |
| 高亮淡出时间 | 15秒 | 5~30秒 | 搜索结果高亮的持续时间 |

### 客户端设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 手持物品搜索 | 关闭 | 无界面时按 Y 搜索手持物品 |
| 搜索音效 | 关闭 | 触发搜索时播放声音 |
| 找到后关闭界面 | 开启 | 搜索到结果后自动关闭当前容器界面 |
| 槽位高亮 | 开启 | 在容器界面中高亮匹配的槽位 |
| 显示容器名称 | 开启 | 在搜索结果旁显示容器名 |

---

## 常见问题

### 搜索没有结果

- 确认你**打开过**包含该物品的容器（纯客户端，不打开不记录）
- 检查搜索范围是否足够大（默认 8 格）
- 确认物品确实在附近容器中，而非远处

### 物品浏览器列表为空

- 需要先打开一些容器，记录才会生成
- 点击 `清除` 按钮会清空所有记录，需重新打开容器

### 投影材料界面列表为空

- 确认已安装 Forgematica (Litematica)
- **必须先在 Litematica 中打开一次材料列表**（按 `N+L`）
- 确认已选中一个投影放置

### 追踪高亮不显示

- 确认追踪已激活（物品浏览器或 HUD 中显示追踪状态）
- 检查追踪范围是否覆盖目标容器（默认 128 格）
- 确认配置中"槽位高亮"已开启

### 追踪 HUD 显示异常数据

- 追踪开始时会自动清零已收集数据
- 结束追踪时也会清理已收集数据
- 如果数据仍有残余，点击 `清除` 按钮重置所有记录

### 双重箱子只记录了一半

- WhereIsIt 会自动合并双重箱子为一条记录
- 如果两个半箱在不同时间打开，可能产生重复记录，重新打开一次即可修正

### 容器内容不更新

- 容器内容只在**关闭界面时**保存快照
- 如果取出/放入物品后没有关闭再打开界面，记录不会更新
